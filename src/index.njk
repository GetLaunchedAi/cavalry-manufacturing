---
layout: layouts/base.njk
title: Home
description: Firearm accessories, components, and medical gear from Cavalry Manufacturing.
---

<section class="hero-full reveal reveal-up">
  <div class="hero-content">
    <div style="display: flex; align-items: center; gap: 12px; margin-bottom: 16px;">
      <img src="/assets/img/products/cavalry-manufacturing-logo-yellow-01.png" alt="Cavalry Logo" style="height: 48px; width: auto;" />
      <span class="kicker">{% icon "shield" %} Cavalry Manufacturing</span>
    </div>
    <h1>Firearm accessories, components, and medical gear built for real‑world use.</h1>
    <p>
      We specialize in advanced manufacturing with a focus on injection molding, producing innovative products
      for shooting sports, military, and law enforcement applications.
    </p>

    <div class="cta">
      <a class="btn primary" href="/collections/">Browse collections {% icon "arrow" %}</a>
      <a class="btn ghost" href="/collections/specials/">Shop specials</a>
      <a class="btn" href="/search/">Search</a>
    </div>

    <div class="kv" style="margin-top:16px;">
      <span class="badge">{% icon "truck" %} Ships to US and US military addresses</span>
      <span class="badge">{% icon "bolt" %} Built for durability and dependable performance</span>
    </div>
  </div>

  <div class="hero-media">
    <img src="/assets/img/products/trauma-kit-contents-layout.jpg" alt="Cavalry Manufacturing Products" />
  </div>
</section>

<section class="section reveal reveal-up">
  <div class="page-header" style="padding:0 0 16px;">
    <span class="kicker">Collections</span>
    <h1 style="font-size: clamp(22px, 2.6vw, 30px); margin-top:10px;">Browse by category</h1>
    <p>Browse firearm accessories and Cavalry Medical essentials.</p>
  </div>

{% set tops = collectionsData | topLevel %}
  <div class="grid cols-3 reveal-group">
    {% for c in tops %}
      {% set c = c %}
      {% include "components/category-card.njk" %}
    {% endfor %}
  </div>

  <div style="height:18px;"></div>

  <div class="card pad reveal reveal-up">
    <div class="kicker">Popular sub-collections</div>
    <div style="height:10px;"></div>
    <div class="grid cols-4">
    {% for c in collectionsData | childrenOf("firearm-accessories") | sortByKey("title") %}
        {% set c = c %}
        {% include "components/category-card.njk" %}
      {% endfor %}
    </div>
  </div>
</section>

<section class="section reveal reveal-up">
  <div class="page-header" style="padding:0 0 16px;">
    <span class="kicker">Featured</span>
    <h1 style="font-size: clamp(22px, 2.6vw, 30px); margin-top:10px;">Featured products</h1>
    <p>Customer favorites across accessories, components, and medical.</p>
  </div>

  {% set featured = products | whereTrue("featured") | sortByDate("updatedAt") %}
  <div class="product-grid reveal-group">
    {% for p in featured %}
      {% set p = p %}
      {% include "components/product-card.njk" %}
    {% endfor %}
  </div>

  <div style="height:18px;"></div>

  <div class="card pad reveal reveal-up">
    <div class="grid cols-2">
      <div>
        <span class="kicker">Promo</span>
        <h2 style="margin:10px 0 8px;">Cavalry Medical — essentials for response</h2>
        <p class="small" style="color:var(--muted); max-width:60ch;">
          Tourniquets, IFAKs, chest seals, and core supplies for rapid care.
        </p>
        <div style="margin-top:12px;">
          <a class="btn primary" href="/collections/cavalry-medical/">Shop medical {% icon "arrow" %}</a>
          <a class="btn" href="/collections/tourniquets/">Tourniquets</a>
        </div>
      </div>
      <div style="border-radius: var(--r-lg); overflow: hidden;">
        <img src="/assets/img/products/trauma-kit-contents-layout.jpg" alt="Medical Supplies" style="width: 100%; height: 100%; object-fit: cover;" />
      </div>
    </div>
  </div>
</section>
