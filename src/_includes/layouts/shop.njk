---
layout: layouts/base.njk
---
<header class="page-header">
  <span class="kicker">{{ kicker or "Store" }}</span>
  <h1>{{ title }}</h1>
  {% if subtitle %}<p>{{ subtitle }}</p>{% endif %}
</header>

{{ content | safe }}
